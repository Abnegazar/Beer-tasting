<h1 class="text-center">Tasting beer</h1>

<div class="rectangle">
    <h4 class="text-center">TASTINGS</h4>
    <div class="card-body">
        <div class="row">
            <div class="col-12">
                <?php
                if (empty($var->tastings) || !isset($var->tastings)) {
                    echo "<p>No tastings </p>";
                } else {
                ?>
                <div class="row">
                    <?php
                        foreach ($var->tastings as $tasting) {
                            echo App::get_partial(PARTIAL_ALL_TASTINGS, array("lastTasting" => $tasting));
                        }

                        ?>
                </div>

                <div class="row text-center">
                    <div class="col-12">
                        <?php
                            if (isset($_GET['mode'])) {
                                $link = PARTIAL_PAGINATION_V3;
                            } else {
                                $link = (isset($_GET['userId'])) ? PARTIAL_PAGINATION_V2 : PARTIAL_PAGINATION_V1;
                            }
                            echo App::get_partial(
                                $link,
                                array(
                                    'pages' => $var->pages,
                                    'count' => $var->count,
                                    'page' => $var->page,
                                )
                            );
                            ?>
                    </div>
                </div>
                <?php
                }
                ?>
            </div>
        </div>
    </div>
</div>